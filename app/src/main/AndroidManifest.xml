<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sun.hotelproject">

    <!--
    <permission
        android:name="com.android.gallery3d.permission.GALLERY_PROVIDER222"
        android:protectionLevel="signatureOrSystem" />

    <uses-permission android:name="com.android.gallery3d.permission.GALLERY_PROVIDER222" />
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 程序主入口 -->
        <activity
            android:name=".moudle.MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".moudle.IdentificationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- android:screenOrientation="landscape"  强制横屏 -->
        <activity
            android:name=".moudle.LayoutHouseActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.ScheduleActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.Not_scheduledActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.PaymentActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.FaceRecognitionActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.VerificationResultActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.CheckOutActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".moudle.PayOutActivity" />
        <activity
            android:name=".moudle.DatePickActivity"
            android:theme="@style/MyDialogStyleBottom" />
        <activity android:name=".moudle.DialogmsgActivity"
            android:theme="@style/MyDialogStyleBottom"/>
        <!--开机广播接受者-->
        <receiver android:name=".receiver.BootBroadcastReceiver">
            <intent-filter>
                <!--注册开机广播地址-->
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>